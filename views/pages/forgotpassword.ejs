<!DOCTYPE html>
<html lang="vi">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quên Mật Khẩu</title>
  <link rel="icon" type="image/png" href="/public/Eden.png">
  <link rel="stylesheet" href="/mycss/cssforgotpassword.css">

</head>

<body>
  <div class="container">
    <h2>Quên Mật Khẩu</h2>
    <p>Vui lòng nhập email của bạn để nhận mã xác nhận.</p>

    <!-- Hiển thị thông báo -->
    <% if (error) { %>
      <div class="message" style="color: <%= error.includes('Mã xác nhận') ? 'red' : 'green' %>;">
        <%= error %>
      </div>
    <% } %>

    <!-- Form nhập email -->
    <form id="emailForm" method="POST" action="/change/sendResetCode"
      class="<%= error && error.includes('Mã xác nhận') ? 'hidden' : '' %>">
      <label for="email">Email</label>
      <input type="email" name="email" placeholder="email@example.com" required>
      <button type="submit">Gửi mã xác nhận</button>
    </form>

    <!-- Form nhập mã xác nhận -->
    <form id="codeForm" method="POST" action="/change/verify-code"
      class="<%= error && error.includes('Mã xác nhận') ? '' : 'hidden' %>">
      <label for="code">Mã xác nhận</label>
      <input type="hidden" name="email" value="<%= email || '' %>">
      <input type="text" name="code" placeholder="Nhập mã xác nhận" required>
      <button type="submit">Xác nhận mã</button>
    </form>

    <a href="/login" class="back-link">Quay lại trang đăng nhập</a>
  </div>
</body>

</html>
